#include "struct.h"
#include "func.h"
#include "keymaps.h"


state run_MAKE_SMALL(machine* mach) {
	return makeCoffee(mach);
}

state run_MAKE_BIG(machine* mach) {
	return makeCoffee(mach);
}

state run_MAKE_ESPRESSO(machine* mach) {
	return makeCoffee(mach);
}

state run_MAKE_CAPUCCINO(machine* mach) {
	return makeCoffee(mach);
}

state run_MAKE_MILK(machine* mach) {
	return makeCoffee(mach);
}